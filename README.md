How to setup
```
  npm install
  npm install sequelize-cli babel-cli -g
  setup server/config/database.json
  create database
  npm run db:migrate
```

How to start
```
  npm run update:schema
  npm run relay
```
